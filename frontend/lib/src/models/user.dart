class User {
  final String id;
  final String userId;
  final String fullname;
  final String role;
  final String status;

  const User(this.id, this.userId, this.fullname, this.role, this.status);

  
}
